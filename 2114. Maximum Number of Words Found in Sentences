class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        n=len(sentences);
        l=[];
        m=0
        for i in sentences:
            
            a=len(i.split())
            if(a>m):
                m=a
        return m;
        
