class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        p=[]
        c=0
        nums.sort();
        for i in nums:
            if i==target:
                p.append(c)
            c+=1
        return p;
