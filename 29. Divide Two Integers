class Solution {
public:
    int divide(int dividend, int divisor) {
        int result=0;
        if (dividend == INT_MIN && divisor == -1) {
            return INT_MAX;
        }
        result=dividend/divisor;
        return result;
    }
};
